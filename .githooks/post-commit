#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
SYNC_SCRIPT="$REPO_ROOT/scripts/sync_installed_gitb.sh"

if [[ ! -x "$SYNC_SCRIPT" ]]; then
  exit 0
fi

if git diff-tree --no-commit-id --name-only -r HEAD | grep -qx "git_bulk.py"; then
  "$SYNC_SCRIPT"
fi
